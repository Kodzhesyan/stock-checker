name: Official Portfolio Analysis

on:
  schedule:
    - cron: '0 22 * * 1-5'
  workflow_dispatch:

jobs:
  analyze-portfolio:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Reconstruct Portfolio
        run: |
          # Создаем портфель и наполняем его
          uv run .agent/skills/stock-analysis/scripts/portfolio.py create "My Portfolio"
          uv run .agent/skills/stock-analysis/scripts/portfolio.py add VOO --quantity 36 --cost 505.20 --portfolio "My Portfolio"
          
          # Проверяем, что портфель виден системе
          uv run .agent/skills/stock-analysis/scripts/portfolio.py list

      - name: Portfolio Analysis with Period Returns
        run: |
          # Добавляем флаг --period (например, weekly или monthly)
          # Это добавит в JSON поля с доходностью за этот период
          uv run .agent/skills/stock-analysis/scripts/analyze_stock.py --portfolio "My Portfolio" --period weekly --output json > portfolio_report.json
          
          cat portfolio_report.json

      - name: Send Deep Analysis to n8n
        env:
          WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: |
          curl -X POST \
               -H "Content-Type: application/json" \
               -d @full_portfolio_report.json \
               $WEBHOOK_URL
